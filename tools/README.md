# Developer tools

Run utilities from the project root as modules, for example:

```powershell
python -m tools.migrate_legacy_data ..\3.9\data
python -m tools.metrics_report
python -m tools.render_ui
```

These utilities are not included in the end-user installer.

